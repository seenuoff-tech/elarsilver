interface ServerlessError {
    message: string;
    code: string;
}
declare class DatabaseError extends Error {
    details: ServerlessError | null;
    status: number;
    constructor(message: string, status: number, details: ServerlessError | null);
}

type Req = {
    method: string;
    headers: Record<string, string>;
    body: string;
    cache?: RequestCache;
};
type Res = {
    ok: boolean;
    status: number;
    statusText: string;
    headers: any;
    json(): Promise<any>;
    text(): Promise<string>;
};
interface Config {
    url?: string;
    username?: string;
    password?: string;
    database?: string;
    host?: string;
    fetch?: (input: string, init?: Req) => Promise<Res>;
    arrayMode?: boolean;
    fullResult?: boolean;
    decoders?: Decoders;
    debug?: boolean;
}
interface ExecuteOptions {
    arrayMode?: boolean;
    fullResult?: boolean;
    decoders?: Decoders;
    debug?: boolean;
}
interface TxOptions {
    isolation?: 'READ COMMITTED' | 'REPEATABLE READ';
}
type ExecuteArgs = object | any[] | null;
declare const enum ColumnType {
    TINYINT = "TINYINT",
    UNSIGNED_TINYINT = "UNSIGNED TINYINT",
    SMALLINT = "SMALLINT",
    UNSIGNED_SMALLINT = "UNSIGNED SMALLINT",
    MEDIUMINT = "MEDIUMINT",
    UNSIGNED_MEDIUMINT = "UNSIGNED MEDIUMINT",
    INT = "INT",
    UNSIGNED_INT = "UNSIGNED INT",
    YEAR = "YEAR",
    FLOAT = "FLOAT",
    DOUBLE = "DOUBLE",
    BIGINT = "BIGINT",
    UNSIGNED_BIGINT = "UNSIGNED BIGINT",
    DECIMAL = "DECIMAL",
    CHAR = "CHAR",
    VARCHAR = "VARCHAR",
    BINARY = "BINARY",
    VARBINARY = "VARBINARY",
    TINYTEXT = "TINYTEXT",
    TEXT = "TEXT",
    MEDIUMTEXT = "MEDIUMTEXT",
    LONGTEXT = "LONGTEXT",
    TINYBLOB = "TINYBLOB",
    BLOB = "BLOB",
    MEDIUMBLOB = "MEDIUMBLOB",
    LONGBLOB = "LONGBLOB",
    DATE = "DATE",
    TIME = "TIME",
    DATETIME = "DATETIME",
    TIMESTAMP = "TIMESTAMP",
    BIT = "BIT",
    JSON = "JSON"
}
type Decoders = {
    [P in ColumnType]?: (rawValue: string) => any;
};

type Row = Record<string, any> | any[];
type Types = Record<string, string>;
interface FullResult {
    types: Types | null;
    rows: Row[] | null;
    statement: string;
    rowCount: number | null;
    rowsAffected: number | null;
    lastInsertId: string | null;
}
interface Field {
    name: string;
    type: string;
    nullable: boolean;
}
declare class Tx<T extends Config> {
    private conn;
    constructor(conn: Connection<T>);
    execute<E extends ExecuteOptions>(query: string, args?: ExecuteArgs, options?: E): Promise<ExecuteResult<E, T>>;
    commit(): Promise<T['fullResult'] extends true ? FullResult : Row[]>;
    rollback(): Promise<T['fullResult'] extends true ? FullResult : Row[]>;
}
type ExecuteResult<E extends ExecuteOptions, T extends Config> = E extends {
    fullResult: boolean;
} ? E['fullResult'] extends true ? FullResult : Row[] : T['fullResult'] extends true ? FullResult : Row[];
declare class Connection<T extends Config> {
    private config;
    private session;
    constructor(config: T);
    getConfig(): Config;
    begin(txOptions?: TxOptions): Promise<Tx<T>>;
    persist(): Promise<StatefulConnection<T>>;
    execute<E extends ExecuteOptions>(query: string, args?: ExecuteArgs, options?: E, txOptions?: TxOptions, statefulAction?: 'open' | 'close'): Promise<ExecuteResult<E, T>>;
}
declare class StatefulConnection<T extends Config> {
    private conn;
    constructor(conn: Connection<T>);
    execute<E extends ExecuteOptions>(query: string, args?: ExecuteArgs, options?: E): Promise<ExecuteResult<E, T>>;
    close(): Promise<void>;
}
declare function connect<T extends Config>(config: T): Connection<T>;

export { ColumnType, type Config, Connection, DatabaseError, type Decoders, type ExecuteArgs, type ExecuteOptions, type ExecuteResult, type Field, type FullResult, type Row, StatefulConnection, Tx, type Types, connect };
